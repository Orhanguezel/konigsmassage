// =============================================================
// FILE: src/components/containers/appointment/AppointmentSection.tsx
// FINAL — Wraps AppointmentPageContent to allow section usage
// =============================================================

'use client';

import React from 'react';
import AppointmentPageContent from './AppointmentPageContent';

const AppointmentSection: React.FC = () => {
  return <AppointmentPageContent />;
};

export default AppointmentSection;
