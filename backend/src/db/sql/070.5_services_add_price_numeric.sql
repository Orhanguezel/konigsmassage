-- =============================================================
-- 070.5 — No-op (price_numeric removed, price is now on parent services table)
-- =============================================================
SELECT 1;
